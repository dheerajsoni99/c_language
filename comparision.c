#include <stdio.h>
int main(){
  int a=10,b=15,c=20;
printf("%d",a>b);//if the comparision is false then it will give value of 0 if the comparision is true then it will output 1 hence printf will execute only one or zero(for false case).
return O;
}
